// Re-export all hooks from domain-specific endpoint files
// This file exists for backward compatibility — new code should import from '@shared/store/endpoints/<domain>'
export * from './endpoints';
